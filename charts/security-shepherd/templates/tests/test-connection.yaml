apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "security-shepherd.fullname" . }}-test-connection"
  labels:
    {{- include "security-shepherd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "security-shepherd.fullname" . }}:{{ .Values.tomcat.port }}']
  restartPolicy: Never
